interface:
  display_name: "Issue Task Flow"
  short_description: "Orchestrate and execute issue-tracked tasks"
  default_prompt: "Use $issue-task-flow to orchestrate the next issue-tracked task. Store the Executor Handoff in Linear, then use a sub-agent Executor when available; otherwise prepare a separate Executor handoff."
