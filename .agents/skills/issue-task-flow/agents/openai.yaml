interface:
  display_name: "Issue Task Flow"
  short_description: "Orchestrate and execute issue-tracked tasks"
  default_prompt: "Use $issue-task-flow to orchestrate the next issue-tracked task. Store implementation and closure handoffs in Linear, use sub-agent Executors when available, and keep the human-facing chat as Orchestrator."
